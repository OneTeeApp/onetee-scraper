name: Discovery probe 6 (recheck verification)

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "scripts/discover6.py"
      - ".github/workflows/discover6.yml"

permissions:
  contents: write

concurrency:
  group: discover6
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps + Chromium
        run: |
          pip install requests beautifulsoup4 playwright
          python -m playwright install --with-deps chromium
      - name: Run probe
        run: |
          mkdir -p probe-results
          python scripts/discover6.py 2>&1 | tee probe-results/discover6.txt
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main || true
          git add probe-results/discover6.txt
          git commit -m "discover6 results [skip ci]" || { echo "no changes"; exit 0; }
          git push
